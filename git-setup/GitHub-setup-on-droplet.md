# 🚀 Git Setup on DigitalOcean Droplet

This document records the steps followed to install and configure Git on a DigitalOcean droplet.

---

## **1️⃣ Install Git**
```sh
sudo apt update
sudo apt install git -y
git --version
